import * as React from 'react';
import Typography from '@mui/material/Typography';

function HerdLogo() {
  return (
    <Typography style={{ textAlign: 'center' }} variant={'h5'}>
      <span style={{ fontStyle: 'italic' }}>
        <strong>Herd</strong>Pay
      </span>
    </Typography>
  );
}

export default HerdLogo;
